@model IEnumerable<EsStore.Models.Product>

@{
  ViewBag.Title = "Product";
  Layout = "_Layout";
}

<h2>Product</h2>

@foreach (var p in Model)
{
  <div>
    <h3>@p.Name</h3>
    @p.Description
    <h4>@p.Price.ToString("C")</h4>
  </div>
}